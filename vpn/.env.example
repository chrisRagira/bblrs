# =============================================================================
# .env — ProxyAaS
# Copy to .env and fill in values. Never commit the real .env.
# =============================================================================

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=proxyaas
DB_USER=proxyaas
DB_PASSWORD=changeme_strong_password

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=changeme_at_least_32_chars_random

# API
PORT=4000
ALLOWED_ORIGINS=*

# Domain
DOMAIN=http://35.209.144.131:3000
